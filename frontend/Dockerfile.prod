FROM node:20-alpine

WORKDIR /app

# Serve pre-built frontend (built locally via ./build.sh)
COPY dist /app/dist

# Install simple HTTP server for serving static files
RUN npm install -g http-server

EXPOSE 5173

CMD ["http-server", "dist", "-p", "5173", "--gzip"]
